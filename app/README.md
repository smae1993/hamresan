# Hamresan Flutter application

The application source lives in this directory. Read the repository-level [README](../README.md), [architecture](../docs/ARCHITECTURE.md), and [protocol specification](../docs/PROTOCOL.md) before making changes.

```bash
flutter pub get
flutter analyze
flutter test
flutter run
```

Web is not a supported target because the transfer engine requires UDP and TCP sockets.
